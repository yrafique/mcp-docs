# never ship secrets or local state into the image
.env
.env.*
!.env.example
**/__pycache__/
*.pyc
.git/
# large seed dump — bind-mounted into docs-db at runtime, never needed in an image
src/doc_store.sql.gz
# marketing/readme assets aren't needed in any image
docs/
